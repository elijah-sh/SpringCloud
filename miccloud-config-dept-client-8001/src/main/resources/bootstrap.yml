spring:
  cloud:
    config:
      name: miccloud-config-dept-client    # 需要从GitHub读取的资源名称，没有后缀名
      profile: dev      # 配置是什么就取什么
      label: master
      uri: http://config-3344.com:3344   #  本微服务启动后先去找3344 号服务，通过springcloudconfig获取GitHub的服务地址